<UserControl x:Class="WpfAppProyectoIntegrador2026_1.Views.MaterialesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfAppProyectoIntegrador2026_1.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid Background="{StaticResource MainBackground}">

        <Border Style="{StaticResource CardStyle}"
                Margin="15"
                Padding="20">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Gestión de Materiales"
                           FontSize="35"
                           FontWeight="Bold"
                           Margin="10,0,0,20"
                           Foreground="{StaticResource PrimaryText}"/>

                <Grid Grid.Row="1"
                      Margin="0,0,0,20">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="txtSearch"
                             Width="320"
                             HorizontalAlignment="Left"
                             Tag="Buscar material..."
                             Style="{StaticResource SearchTextBox}"
                             TextChanged="TxtSearch_TextChanged"/>

                    <Button Grid.Column="1"
                            Content="Agregar Material"
                            Width="170"
                            Style="{StaticResource PrimaryButton}"
                            Click="BtnAdd_Click"/>

                </Grid>

                <DataGrid x:Name="tableMateriales"
                          Grid.Row="2"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          BorderThickness="0"
                          SelectionMode="Single"
                          HorizontalGridLinesBrush="#E5E7EB">

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="ID"
                                            Binding="{Binding Id}"
                                            Width="120"/>

                        <DataGridTextColumn Header="Nombre"
                                            Binding="{Binding Nombre}"
                                            Width="*"/>

                        <DataGridTextColumn Header="Precio Unidad"
                                            Binding="{Binding PrecioUnidad}"
                                            Width="180"/>

                        <DataGridTextColumn Header="Descripción"
                                            Binding="{Binding Descripcion}"
                                            Width="250"/>

                        <DataGridTextColumn Header="Estado"
                                            Binding="{Binding Estado}"
                                            Width="120"/>

                        <DataGridTemplateColumn Header="Acciones"
                                                Width="120">

                            <DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <Button Content="Editar"
                                            Width="90"
                                            Style="{StaticResource GridButton}"
                                            Click="BtnEdit_Click"/>

                                </DataTemplate>

                            </DataGridTemplateColumn.CellTemplate>

                        </DataGridTemplateColumn>

                    </DataGrid.Columns>

                </DataGrid>

            </Grid>

        </Border>

    </Grid>

</UserControl>